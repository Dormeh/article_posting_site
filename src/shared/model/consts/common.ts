export enum ContentView {
    PLATE = 'plate',
    LIST = 'list',
}

export enum SortOrder {
    ASC = 'asc',
    DESC = 'desc',
}

export enum UserRole {
    ADMIN = 'ADMIN',
    MANAGER = 'MANAGER',
    USER = 'USER',
}
